import { FC } from 'react';
import { Text, View } from 'react-native';

const Temp: FC = () => {
    return (
        <View>
            <Text>Temp</Text>
        </View>
    );
};

export default Temp;
